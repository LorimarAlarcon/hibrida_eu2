import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ph.evaluacion2.u2',
  appName: 'ph-evaluacion2-u2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
